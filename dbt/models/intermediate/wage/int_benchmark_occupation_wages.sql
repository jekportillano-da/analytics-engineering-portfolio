{{ config(enabled=target.type == 'bigquery') }}

select
    logical_observation_id,
    source_record_id,
    semantic_dataset_id,
    matrix_id,
    matrix_title,
    'benchmark_occupation_wages' as matrix_scope,
    reference_year,
    industry_code,
    regexp_replace(industry_name, r'^\.\.', '') as industry_name,
    industry_name as source_industry_name,
    cast(null as string) as geography_type,
    cast(null as string) as geography_code,
    cast(null as string) as geography_name,
    matrix_id as benchmark_occupation_id,
    occupation_name,
    sex_code,
    sex,
    measure_code as source_measure_code,
    source_measure_name,
    'benchmark_wage_rate' as wage_measure_id,
    'Benchmark Occupation Wage Rate' as governed_measure_name,
    observation_value,
    observation_status,
    source_unit,
    source_publisher,
    source_updated_at,
    source_artifact_id,
    retrieval_id,
    extraction_id,
    request_id,
    source_record_locator,
    identifier_version,
    matrix_metadata_id,
    canonical_endpoint,
    requested_format,
    sha256_checksum,
    storage_key,
    first_retrieved_at,
    retrieval_started_at,
    retrieval_completed_at,
    loaded_at
from {{ ref('stg_wage_ows_observations') }}
where matrix_id in ('0051B3E2005.px', '0071B3E2007.px')
